overwrite: true
schema: "./src/**/*.graphql"
documents: null
generates:
    ./src/resolvers-types.ts:
        config:
            declarationKind:
                type: "class"
                input: "class"
        plugins:
            - typescript
            - typescript-resolvers
