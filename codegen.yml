overwrite: true
schema: "./src/**/*.schema.ts"
documents: null
generates:
    ./src/resolvers-types.ts:
        config:
            declarationKind:
                type: "class"
                input: "class"
        plugins:
            - typescript
            - typescript-resolvers
