overwrite: true
schema: "./schema.ts"
documents: null
generates:
  ./src/resolvers-types.ts:
    config:
      declarationKind: 
        type: 'class'
        input: 'class'
    plugins:
      - typescript
      - typescript-resolvers